apiVersion: 1

providers:
  - name: "Prometheus"
    folder: ""
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 10
    options:
      path: "/var/lib/grafana/dashboards"
